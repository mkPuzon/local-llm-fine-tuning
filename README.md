# Fine-Tuning Suite and Tests

Fine tuning Qwen 3.5 family models using Unsloth. Example code for fine-tuning LLMs locally using Unsloth's Pytohon API in `notebooks/` directory. All tests were run on one NVIDIA A100 GPU.

Results and analysis can be founds in the `notes/` directory: [[notes/qwen3.5-eb-guardian-results.md]].